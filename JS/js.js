//=========================================-1-====================================function getNumbers(myArray){    var newArray = [];    myArray.forEach(function (e){        if (!isNaN(parseInt(e))){            newArray.push(parseInt(e));        }    });    return newArray;}//==========================================-2-===================================function getNumericSum(myArray){    var newArray = [];    myArray.forEach(function (e){        if (!isNaN(parseInt(e))){            newArray.push(parseInt(e));        }    });    var result = newArray.reduce(function(sum, current){        return sum + current;    }, 0);    return result}//==========================================-4-====================================function getQuanOfUniqueCharacters(myString) {    var myArray = myString.split("");    myArray.sort();    for (var i = 0; i < myArray.length; i++) {        while (myArray[i] === myArray[i + 1]) {            myArray.splice(i, 1);        }    }    return myArray.length;}//=========================================-5-=====================================function getPensionersName(human) {    var pensionersArray = [];    for (var i = 0; i < human.length; i++) {        if (human[i].age > 65) {            pensionersArray.push(human[i].name);        }    }    return pensionersArray;}